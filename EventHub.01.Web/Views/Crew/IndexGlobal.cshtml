@model IEnumerable<EventHub._02.Bussines.DTOs.OperadorDto>
@{
    ViewBag.Title = "Operadores";
    ViewBag.HeaderTitle = "Operadores";
    ViewBag.HeaderSubtitle = "Gestión global de operadores del sistema";
    ViewBag.BackUrl = Url.Action("Index", "Home");
}

@section headerActions {
    <div class="section-header__actions">
        <button class="btn btn-primary btn-sm" onclick="openCrearOperadorModal()">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
            Nuevo Operador
        </button>
    </div>
}

<div class="card">
    <table style="width:100%; border-collapse:collapse;">
        <thead>
            <tr style="border-bottom:1px solid var(--border); text-align:left;">
                <th style="padding:12px 16px; font-size:12px; font-weight:600; color:var(--text-muted);">Nombre</th>
                <th style="padding:12px 16px; font-size:12px; font-weight:600; color:var(--text-muted);">Cédula</th>
                <th style="padding:12px 16px; font-size:12px; font-weight:600; color:var(--text-muted);">Email</th>
                <th style="padding:12px 16px; font-size:12px; font-weight:600; color:var(--text-muted);">Rol</th>
                <th style="padding:12px 16px; font-size:12px; font-weight:600; color:var(--text-muted);">Estado</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Any())
            {
                foreach (var o in Model)
                {
                    <tr style="border-bottom:1px solid var(--border);">
                        <td style="padding:12px 16px; font-size:13px; font-weight:500;">@o.Nombre</td>
                        <td style="padding:12px 16px; font-size:13px; color:var(--text-muted);">@(o.Cedula ?? "-")</td>
                        <td style="padding:12px 16px; font-size:13px;">@o.Email</td>
                        <td style="padding:12px 16px; font-size:13px;">@(o.Rol ?? "-")</td>
                        <td style="padding:12px 16px;">
                            <span style="display:inline-block; padding:2px 8px; border-radius:12px; font-size:11px; font-weight:600; @(o.Estado ? "background:#d1fae5; color:#065f46;" : "background:#fee2e2; color:#991b1b;")">
                                @(o.Estado ? "Activo" : "Inactivo")
                            </span>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="5" style="padding:32px; text-align:center; color:var(--text-muted); font-size:13px;">No hay operadores registrados</td>
                </tr>
            }
        </tbody>
    </table>
</div>

<!-- Modal Crear Operador -->
<div id="crearOperadorModal" class="modal-overlay" style="display:none; position:fixed; inset:0; background:rgba(15,23,42,0.5); z-index:2000; align-items:center; justify-content:center;">
    <div style="width:450px; max-width:95vw; background:var(--bg-card); border-radius:12px; overflow:hidden; box-shadow:0 20px 60px rgba(0,0,0,0.3);">
        <div style="display:flex; align-items:center; justify-content:space-between; padding:20px 24px; border-bottom:1px solid var(--border);">
            <h3 style="margin:0; font-size:18px; font-weight:700;">Nuevo Operador</h3>
            <button onclick="closeCrearOperadorModal()" style="background:none; border:none; cursor:pointer; color:var(--text-muted);">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
            </button>
        </div>
        <form id="crearOperadorForm" style="padding:24px;">
            @Html.AntiForgeryToken()
            <div class="form-group">
                <label style="font-size:13px; font-weight:600;">Nombre *</label>
                <input type="text" name="Nombre" class="form-control" required maxlength="150" />
            </div>
            <div class="form-group">
                <label style="font-size:13px; font-weight:600;">Cédula</label>
                <input type="text" name="Cedula" class="form-control" maxlength="20" />
            </div>
            <div class="form-group">
                <label style="font-size:13px; font-weight:600;">Email *</label>
                <input type="email" name="Email" class="form-control" required maxlength="200" />
            </div>
            <div class="form-group">
                <label style="font-size:13px; font-weight:600;">Teléfono</label>
                <input type="text" name="Telefono" class="form-control" maxlength="20" />
            </div>
            <div class="form-group">
                <label style="font-size:13px; font-weight:600;">Rol</label>
                <input type="text" name="Rol" class="form-control" maxlength="100" placeholder="Ej: Técnico, DJ, Coordinador" />
            </div>
            <div style="display:flex; gap:8px; justify-content:flex-end; margin-top:20px;">
                <button type="button" class="btn btn-outline" onclick="closeCrearOperadorModal()">Cancelar</button>
                <button type="submit" class="btn btn-primary">Crear Operador</button>
            </div>
        </form>
    </div>
</div>

<script>
    function openCrearOperadorModal() {
        document.getElementById('crearOperadorForm').reset();
        document.getElementById('crearOperadorModal').style.display = 'flex';
    }
    function closeCrearOperadorModal() {
        document.getElementById('crearOperadorModal').style.display = 'none';
    }

    document.getElementById('crearOperadorForm').addEventListener('submit', function(e) {
        e.preventDefault();
        var fd = new FormData(this);
        fetch('@Url.Action("CrearOperadorAjax", "Crew")', {
            method: 'POST',
            body: fd
        })
        .then(r => r.json())
        .then(data => {
            if (data.success) {
                window.EventHubSwal.success('Operador creado correctamente.');
                location.reload();
            } else {
                window.EventHubSwal.error('Error: ' + data.message);
            }
        });
    });
</script>
